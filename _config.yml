# Dependencies
# Force kramdown instead of commonmark (GitHub Pages default)
markdown: kramdown
highlighter: rouge

# Use custom layouts (no theme)
# GitHub Pages may auto-detect theme, but custom layouts take precedence

# Kramdown configuration
kramdown:
  input: GFM
  syntax_highlighter: rouge
  hard_wrap: false
  auto_ids: true
  footnote_nr: 1
  entity_output: as_char
  toc_levels: 1..6
  smart_quotes: lsquo,rsquo,ldquo,rdquo
  enable_coderay: false

# Permalinks
permalink: pretty

# Setup
title: The ദloj
tagline: "Notes from other times"
description: "Backend Engineer specializing in high-performance architectures and clean code."
url: https://uliber.com
baseurl: /

author:
  name: "Raúl JM"
  url: https://www.linkedin.com/in/raul-jimenez-martin-radw2020/
paginate: 30

# Plugins (GitHub Pages compatible)
plugins:
  - jekyll-paginate

# Custom vars
version: 2.1.0

github: "radw2020"
